#pragma once
#include <string>

void readC(); //prints the list of all courses(non specific)

void checkCourses(); //checks the courses and fills a vector at the start of the program

void sortS(bool); //sorts and prints the courses for the specified semester

void cSearch(std::string &);

void cCalculate(std::string&);